# 의존성
node_modules/

# 빌드 산출물
dist/

# 환경변수 — Supabase 키가 들어간다
.env
.env.local
.env.*.local

# 엑셀에서 뽑아낸 임시 반입 데이터. 업체 담당자 연락처가 들어 있어 커밋하지 않는다.
# 마이그레이션에 옮겨 담고 나면 지워도 되는 파일이다.
.excel-import.json

# 에디터 · OS
.vscode/
.idea/
.DS_Store
Thumbs.db
